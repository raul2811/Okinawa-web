{
  "name": "Reflex Dev Environment",
  "build": {
    "dockerfile": "../Dockerfile",
    "context": ".."
  },
  "postCreateCommand": "cd app/web && reflex run",
  "forwardPorts": [3000, 8000],
  "settings": {
    "terminal.integrated.defaultProfile.linux": "fish"
  },
  "remoteUser": "root"
}

